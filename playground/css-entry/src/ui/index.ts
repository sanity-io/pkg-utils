/**
 * The stylesheet ships as the `./ui/styles.css` export subpath, so consumers opt into it with
 * `import "css-entry/ui/styles.css"` rather than it being pulled in here.
 * @public
 */
export const tableClassName = 'table'
