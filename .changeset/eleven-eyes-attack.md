---
"@sanity/tsdown-config": minor
---

feat: expose tsdown's top-level `target` option, passed through as-is like `format`, `dts` and `define`. It downlevels JS syntax for the given runtimes and doubles as the default CSS syntax lowering target when `vanillaExtract` is enabled
