{
  "name": "@css-playground/parcel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "parcel build src/index.html --dist-dir dist --no-cache",
    "clean": "rimraf dist .parcel-cache",
    "test": "parcel build src/index.html --dist-dir dist --no-cache"
  },
  "@parcel/resolver-default": {
    "packageExports": true
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "parcel": "^2.16.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "rimraf": "^6.1.3",
    "sanity-css-vanilla-extract-test": "workspace:*"
  }
}
