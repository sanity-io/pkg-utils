---
'@sanity/pkg-utils': minor
---

Add `extract.checkTypes` option to control type checking during dts generation. When set to false, it has the same effect as setting `"noCheck": true` in tsconfig.json.
