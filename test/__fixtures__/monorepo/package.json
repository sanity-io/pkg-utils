{
  "private": true,
  "name": "monorepo",
  "version": "0.0.0-development",
  "license": "MIT",
  "main": "./package.json",
  "scripts": {
    "build": "pnpm run --recursive build"
  },
  "devDependencies": {
    "esbuild": "^0.15.5",
    "esbuild-register": "^3.3.3",
    "typescript": "^4.7.4"
  }
}
