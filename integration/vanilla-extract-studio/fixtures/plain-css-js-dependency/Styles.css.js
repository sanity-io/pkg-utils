// A plain JS module exporting a CSS string — NOT a vanilla-extract module — whose file name
// matches vanilla-extract's `cssFileFilter` (like `@bynder/compact-view`'s `Styles.css.js`).
const e = '.plain-css-js-dependency{color:rgb(201, 202, 203)}'
export {e as default}
